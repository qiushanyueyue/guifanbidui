# 仓库代理工作协议

本文件是进入本仓库的新对话、新代理和子代理的强制入口。它约束整个仓库；更深层目录如有自己的 `AGENTS.md`，只补充其局部规则。

## 新对话启动：最多五步

1. 阅读 [`docs/PROJECT_CHARTER.md`](docs/PROJECT_CHARTER.md)，确认使命、边界和不可破坏原则。
2. 阅读 [`MEMORY.md`](MEMORY.md) 与 [`docs/project-memory/CURRENT.md`](docs/project-memory/CURRENT.md)，获取当前事实、风险和证据时间。
3. 只按任务需要读取 [`docs/project-memory/decisions/`](docs/project-memory/decisions/) 中相关 ADR；需要追溯时再查 [`HISTORY.md`](docs/project-memory/HISTORY.md)。
4. 对生产状态、数据量、分支、部署、外部来源等会变化的事实做实时只读核验；记忆不是实时证据。
5. 检查 `git status`、适用测试和用户已有修改，然后再编辑；不得清理、覆盖或回退无关改动。

## 事实与文档优先级

1. 用户当前明确要求与实时核验结果。
2. 本文件、项目纲领、`MEMORY.md`、`CURRENT.md` 和有效 ADR。
3. 当前代码、测试、数据库迁移与部署配置。
4. `README.md`。
5. `PROJECT_HANDOFF.md`、`PROJECT_SUMMARY.md` 等历史材料；它们可能过时，不能单独作为当前事实。

发生冲突时，以较新的已验证证据为准，同时在 `HISTORY.md` 记录旧事实失效，不能静默删除历史。

## 不可破坏的产品规则

- 普通规范搜索只查询数据库；现场抓取只属于显式同步、核验或受保护操作。
- 设计说明优先本地提取。只有本地一条规范也未提取到、管理员显式开启且服务端 Secret 存在时，才允许远程兜底。
- `unknown`、`conflict`、来源失败或未核验不得表述为“现行”。第三方查询结果不得冒充官方权威结论。
- 保留规范身份、版次、局部修订、替代关系、来源证据和核验级别；不得只按年份猜测版本。
- 不得把图纸、完整内部设计说明、用户文件或数据库内容发送给 WorkBuddy 等只读查新接口；其输入边界是规范名称和编号。
- 不得在代码、文档、记忆、日志、截图或提交中保存密码、API Key、Token、Recovery Code、Cookie、数据库连接串或个人隐私。

## 记忆更新协议

以下任一事项发生即属于“耐久变更”，本次任务结束前必须更新项目记忆：

- 产品范围、架构、API、数据模型、状态语义或安全边界改变；
- 数据导入、来源核验、生产部署、域名、分支或自动化状态改变；
- 用户流程、关键测试基线、已知风险或阻塞状态改变；
- 新增会影响后续实现的长期决定或推翻旧决定。

更新方式：

1. 当前真相改变：原位更新 `docs/project-memory/CURRENT.md` 的 `as_of`、事实、证据和核验级别。
2. 每次耐久变更：在 `docs/project-memory/HISTORY.md` 追加一条，不改写旧条目。
3. 长期技术/产品取舍：新增 ADR；旧 ADR 标为 `superseded`，不得删除。
4. 更新根目录 `MEMORY.md` 的紧凑摘要、风险或索引；不要把会话流水账复制进去。
5. 运行 `python scripts/check_project_memory.py`。

只改注释、格式、临时调试或没有形成耐久事实时，可在收尾报告中明确写“无需更新项目记忆”及原因。

## 完成定义

完成不等于代码已修改。交付前必须：

- 运行与风险相称的目标测试、完整测试、lint/build 和真实用户流程；不能把编译通过当成功能验收。
- 明确区分：已实现、测试通过、生产实测、尚未验证。
- 保留用户脏工作树，只暂存本任务确认文件。
- 按上节判断并更新记忆；耐久变更未更新记忆视为未完成。
- 运行 `python scripts/check_project_memory.py` 与 `git diff --check`。
- 报告改了什么、如何验证、剩余风险或阻塞。
